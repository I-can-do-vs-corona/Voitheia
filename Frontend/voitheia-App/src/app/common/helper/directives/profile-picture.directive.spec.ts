import { ProfilePictureDirective } from './profile-picture.directive';

describe('ProfilePictureDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfilePictureDirective();
    expect(directive).toBeTruthy();
  });
});
